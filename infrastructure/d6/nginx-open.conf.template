events {}
http {
  upstream poweriot_backend { server backend:8080; }
  server {
    listen 8081;
    location / {
      proxy_pass http://poweriot_backend;
      proxy_set_header Host $host;
    }
  }
}
